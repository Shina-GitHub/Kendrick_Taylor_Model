#The Kendrick Taylor model is an economic growth model. It is establishes 
#the relationship between consumption and captial
require(MASS)
final_time = seq(from =20,to =100, by=10)
len_final_time = length(final_time)